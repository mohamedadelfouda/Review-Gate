#!/usr/bin/env bash
# review-gate dogfood pre-push hook (runs the source gate directly).
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || true)"
[ -n "$ROOT" ] && [ -f "$ROOT/gate/review-gate.sh" ] || exit 0
exec bash "$ROOT/gate/review-gate.sh" prepush
