{
  "$comment": "Example for a Go project (go vet + golangci-lint + go test). Save as .review-gate/gate.config.json. If MERGING into an existing config, copy only verify/extensions and KEEP your own gateMode.",
  "gateMode": "commit",
  "verify": {
    "typecheck": {
      "cmd": "go vet ./...",
      "perFile": false,
      "enabled": true
    },
    "lint": {
      "cmd": "golangci-lint run",
      "perFile": false,
      "enabled": true
    },
    "test": {
      "cmd": "go test ./...",
      "perFile": false,
      "enabled": true
    }
  },
  "lintableExtensions": ["go"],
  "codeExtensions": ["go"]
}
