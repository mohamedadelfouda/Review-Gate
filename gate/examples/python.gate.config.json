{
  "$comment": "Example for a Python project (mypy + ruff + pytest). Save as .review-gate/gate.config.json. If MERGING into an existing config, copy only verify/extensions and KEEP your own gateMode.",
  "gateMode": "commit",
  "verify": {
    "typecheck": {
      "cmd": "python -m mypy .",
      "perFile": false,
      "enabled": true
    },
    "lint": {
      "cmd": "python -m ruff check",
      "perFile": true,
      "enabled": true
    },
    "test": {
      "cmd": "python -m pytest -q",
      "perFile": false,
      "enabled": true
    }
  },
  "lintableExtensions": ["py"],
  "codeExtensions": ["py"]
}
