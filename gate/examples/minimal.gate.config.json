{
  "$comment": "Minimal: no typecheck/lint, only the whole test suite. Use when the project has no typechecker/linter yet. The review + guard-skill acknowledgment still applies. Save as .review-gate/gate.config.json; if MERGING, keep your own gateMode.",
  "gateMode": "commit",
  "verify": {
    "typecheck": { "enabled": false },
    "lint": { "enabled": false },
    "test": { "cmd": "npm test --silent", "perFile": false, "enabled": true }
  },
  "lintableExtensions": ["ts", "tsx", "js", "jsx"],
  "codeExtensions": ["ts", "tsx", "js", "jsx", "py", "go", "rs", "rb", "sh"]
}
